import { NOT_ADMIN_ERR_MSG, UNAUTHED_ERR_MSG } from '@shared/const';
import { initTRPC, TRPCError } from "@trpc/server";
import superjson from "superjson";
import type { TrpcContext } from "./context";
import { ENV } from "./env";

const t = initTRPC.context<TrpcContext>().create({
  transformer: superjson,
});

export const router = t.router;
export const publicProcedure = t.procedure;

const requireUser = t.middleware(async opts => {
  const { ctx, next } = opts;

  if (!ctx.user) {
    throw new TRPCError({ code: "UNAUTHORIZED", message: UNAUTHED_ERR_MSG });
  }

  return next({
    ctx: {
      ...ctx,
      user: ctx.user,
    },
  });
});

export const protectedProcedure = t.procedure.use(requireUser);

export const adminProcedure = t.procedure.use(
  t.middleware(async opts => {
    const { ctx, next } = opts;
    const ownerHeader = ctx.req.headers["x-owner-dashboard-key"];
    const ownerKey =
      typeof ownerHeader === "string"
        ? ownerHeader
        : Array.isArray(ownerHeader)
          ? ownerHeader[0]
          : undefined;

    const isRoleAdmin = Boolean(ctx.user && ctx.user.role === "admin");
    const isOwnerKeyValid = Boolean(
      ENV.ownerDashboardKey && ownerKey && ownerKey === ENV.ownerDashboardKey
    );

    if (!isRoleAdmin && !isOwnerKeyValid) {
      throw new TRPCError({ code: "FORBIDDEN", message: NOT_ADMIN_ERR_MSG });
    }

    return next({
      ctx: {
        ...ctx,
        user: ctx.user,
      },
    });
  }),
);
